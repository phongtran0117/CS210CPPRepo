09/11
for the pop() and peek(), I decided to choose my reurn as -1, so it's easier for me to see it as an error


09/15

I had a bug with the pop() function and it returned the wrong value. the fix was just returning poppedValue instead of data[topIndex]. Something that I'd do differently is do one function at a time and test. 

09/15